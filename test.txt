#include <stdio.h>
#include <string.h>

int main() {
    char password[80];
    char usernames[80];
    FILE *file;

    if ((file = fopen("test.txt", "a")) == NULL) {
        fprintf(stderr, "cannot open test.txt\n");
        return 1;
    }

    printf("Enter your password: ");
    scanf("%s", password);
    printf("Size of your password: %d\n", (int)strlen(password));
    printf("Enter the contents of file: \n");
    fgets(password, 80, stdin);


    //Write your input in file
    fputs(password, file);

    while (fgets(password, sizeof password, file)) {
        if (password[strspn(password, " ")] == '\n') 
            continue;
        else 
            fprintf(stderr, "invalid line: %s", password);

    }
    fclose(file);

    
    return 0;
}
